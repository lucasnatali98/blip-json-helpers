import { Request, Router, Response } from "express";

const blipEventTrackingRouter = Router();

blipEventTrackingRouter.get("", (req: Request, res: Response) => {});

export default blipEventTrackingRouter;
