import { expect, describe, it } from "@jest/globals";

describe("Development Tools - Tags - Suite de Testes", () => {
  describe("Casos de sucesso", () => {
    it("Deve inserir as Tags em todos os blocos", () => {
      // Arrange
      //Act
      //Assert
    });
    it("Deve remover as Tags em todos os blocos", () => {
      // Arrange
      //Act
      //Assert
    });
    it("Deve atualizar as Tags em todos os blocos", () => {
      // Arrange
      //Act
      //Assert
    });
  }),
    describe("Casos de falha", () => {
      it("Deve falhar ao inserir as Tags em todos os blocos", () => {
        // Arrange
        //Act
        //Assert
      });
      it("Deve falhar ao remover as Tags em todos os blocos", () => {
        // Arrange
        //Act
        //Assert
      });
      it("Deve falhar ao atualizar as Tags em todos os blocos", () => {
        // Arrange
        //Act
        //Assert
      });
    });
});
